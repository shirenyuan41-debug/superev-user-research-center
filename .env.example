# Frontend API configuration for local development.
# Usually you can leave these empty and rely on the Vite proxy.
VITE_API_BASE_URL=""
VITE_API_PROXY_TARGET="http://127.0.0.1:3001"

# Vercel 同域部署时可保持为空；如果前后端分域部署，请改成后端域名。
# VITE_API_BASE_URL="https://your-api-domain.example.com"
